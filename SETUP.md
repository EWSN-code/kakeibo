## 家計簿 v1.7.0e 更新手順

```bash
git add .
git commit -m "v1.7.0e モバイル推移グラフと色プリセット改善"
git push
```

### 変更点
- スマホでは月別推移を直近月側から表示
- 横スクロールで過去月へ移動
- スマホ向けに月幅とグラフ高さを調整
- 凡例を2列、狭い端末は1列にして崩れを修正
- カテゴリ設定へ色プリセットを追加
- 大カテゴリ同士が重ならない3種類の配色を用意
- 中・小カテゴリは親色を基準に派生色を生成
