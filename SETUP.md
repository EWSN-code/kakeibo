## 家計簿 v1.6.2b 更新手順

```bash
git add .
git commit -m "v1.6.2b カテゴリ管理復旧とレシートUI改善"
git push
```

### 変更点
- v1.6.1相当のカテゴリ管理ブロックを復旧
- カテゴリ一覧内の＋挿入、統合、分割、順序変更を復旧
- レシート入力モーダルをPCでは横幅いっぱいに近い幅へ変更
- レシート入力モーダルでは外側クリックで閉じないように変更
- v1.6.2a の簡易式入力 / OCRカードUI / AIカテゴリ一覧プロンプト / 外税税率別小計は維持
- src/app/*.part.js も v1.6.2b に更新
