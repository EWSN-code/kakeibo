### 家計簿 v1.6.2r 更新手順

```bash
git add .
git commit -m "v1.6.2r 店舗別支払デフォルトと単独現物券対応"
git push
```

- 新規入力で、店ごとの過去支払総額が最大の支払手段を自動選択
- 単独支払でも現物券を選択可能
- 単独現物券で枚数・額面減少額・今回の利用価値を入力可能
- 単独支払から複数支払へ切替時、支払手段と金額を引継ぎ
