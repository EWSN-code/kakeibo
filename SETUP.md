## 家計簿 v1.7.0f 更新手順

```bash
git add .
git commit -m "v1.7.0f 同階層配色とカード請求月・ポイント収支修正"
git push
```

### 変更点
- 同じ親に属する中カテゴリ・小カテゴリを別系統色へ割当
- 円グラフ、推移グラフ、一覧の色参照をフルカテゴリパスに統一
- カード支出ごとに請求締め月を手動指定可能
- 購入日は維持し、カード請求サイクルだけ移動
- 管理外ポイント控除を経済収支の支出から控除
- 消費総額、管理外ポイント控除、純支出を分けて表示
