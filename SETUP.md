# 家計簿 v1.1 セットアップ（更新版）

## 更新方法（GitHub Pages・push一発）
1. このフォルダの中身で、既存リポジトリのファイルを上書き（同名で置換）
2. VSCodeのターミナルで：
   git add .
   git commit -m "v1.1 スマホUI最適化（下タブ/ドロワー/カテゴリ改善）"
   git push
3. 数十秒で https://ewsn-code.github.io/kakeibo/ に反映
4. スマホでアプリを開き、更新を反映（PWAは再読み込みで更新／必要なら一度アプリを閉じて再起動）

## 変更点（v1.1 フェーズ1）
- スマホ：下部タブ（入力/一覧/分析/予算/≡メニュー）＋≡でメニュー（残高・口座/カード/ほしいもの/価格比較/レポート/設定）
- 入力画面が最初に開く（サマリは下に折りたたみ）
- ほしいもの・価格比較が別画面に分離
- カテゴリ選択：スマホは小カテゴリのみ表示・候補は2行

## 次回（フェーズ2予定）
- 金額入力パッド（電卓＋レシートモード）：スマホで数式が打てない問題を解決
