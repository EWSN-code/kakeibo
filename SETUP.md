# 家計簿 v1.2 更新手順（GitHub Pages）

## 更新（push一発）
1. このフォルダの中身で既存リポジトリのファイルを上書き（同名で置換）
2. VSCodeのターミナルで：
   git add .
   git commit -m "v1.2 入力サマリ撤去 / 分析タブ（収支・費用・収入）"
   git push
3. 数十秒で https://ewsn-code.github.io/kakeibo/ に反映
4. スマホは再読み込み（PWAは一度閉じて再起動すると確実）

## 変更点（v1.2）
- 入力タブから「今月サマリ」を撤去（入力に集中）
- タブ「カテゴリ分析」→「分析」に改称
- 分析タブ右上を「収支 / 費用 / 収入」に。既定は「収支」
  - 収支＝俯瞰（収入/支出/収支/取引数カード＋収入vs支出バー＋費用ドーナツ＋収入内訳、掘り下げリンク付き）
  - 費用/収入＝従来のドリルダウン（大→中→小・予算バー・明細）

## 注意
- config.js は前回同様あなたのSupabase設定入り。そのまま動きます。
