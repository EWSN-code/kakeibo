# 家計簿 v1 セットアップ手順（Supabase同期・PWA）

## 0. 何が変わったか
- **クラウド同期**：どの端末からでも同じデータ（Supabase）。
- **ログイン**：メール＋パスワード。共有アカウントで家族2人でも同じ家計簿。
- **PWA**：スマホのホーム画面に追加すると、アイコンから即起動（2回目以降はログイン省略）。
- **カテゴリ検索入力**：カテゴリ欄が「検索して選ぶ」コンボボックスに。
- **読み仮名の手動編集**：設定タブで表示名→よみを管理（漢字もひらがな検索）。

## 1. ファイル一覧
```
index.html          … 起動ページ
config.js           … Supabase接続設定（URL・公開キー）※あなた専用に設定済み
cloud.js            … 認証＋クラウド同期＋ログイン画面＋PWA登録
model.js            … 会計エンジン（無改修）
app.js              … UI本体
styles.css          … スタイル
manifest.json       … PWA定義
sw.js               … Service Worker（ホーム画面追加・アプリ殻キャッシュ）
icon-192.png / icon-512.png / apple-touch-icon.png / icon-maskable-512.png … アイコン
```

## 2. 公開（ホスティング）— ここが必須
⚠️ **PWA（ホーム画面追加）と認証は、HTTPSで配信しないと正しく動きません**。`index.html` をローカルのファイル（file://）で直接開くと Service Worker が登録できず、ホーム画面追加も不安定です。**無料で置ける場所**にアップしてください。おすすめ2択：

### 案A：GitHub Pages（今GitHubを使っているのでこれが自然）
1. このフォルダ一式を、あなたのGitHubリポジトリに push（既存の家計簿リポジトリでOK）。
2. リポジトリの **Settings → Pages** で、Branch を `main`（フォルダは `/root` かファイルを置いた場所）に設定して Save。
3. 数十秒後、`https://<ユーザー名>.github.io/<リポジトリ名>/` で公開されます。
4. そのURLをスマホで開く → 下記「4. ホーム画面に追加」へ。

### 案B：Netlify Drop（コード管理不要・一番手早い）
1. https://app.netlify.com/drop を開く。
2. このフォルダ**ごと**ドラッグ＆ドロップ。
3. 発行された `https://xxxx.netlify.app/` がそのまま使えます。

> どちらも無料。独自ドメインは不要です。

## 3. 初回ログイン＆データ移行
1. 公開URLを開くと**ログイン画面**が出ます。
2. 初回は「**新規アカウント作成**」でメール＋パスワード（6文字以上）を登録
   （Supabaseで確認メールをOFFにしてあるので、その場でログインされます）。
3. **これまでこの端末のブラウザで使っていたデータがあれば、初回ログイン時に自動でクラウドへ移行**されます。
   - 別端末の分を移したい場合は、旧端末で「設定→JSON書き出し」→新端末で「JSON読み込み」でもOK。

## 4. スマホのホーム画面に追加（アイコンで起動）
- **iPhone (Safari)**：共有ボタン（□↑）→「**ホーム画面に追加**」。
- **Android (Chrome)**：メニュー（⋮）→「**ホーム画面に追加** / アプリをインストール」。
- 追加後はアイコンから**全画面アプリ**として起動。2回目以降は**ログイン画面をスキップ**して即表示されます。

## 5. 家族（お母さん）と共有する（A案：共有アカウント）
- お母さんのスマホで**同じ公開URL**を開き、**あなたと同じメール・パスワードでログイン**。
- そのままホーム画面に追加すれば、以後アイコン起動で**同じ家計簿**が見られます。
- どちらの端末で入力しても、少し待てば（保存の少し後に）もう一方にも反映されます
  ※現状は「開いたときに最新を読む」方式。開きっぱなしの自動反映は将来のオフライン対応時に強化予定。

## 6. 使い勝手メモ
- ヘッダー右に同期状態（**同期中… / 同期済み**）とログイン中メール、**ログアウト**ボタン。
- **カテゴリ欄は検索**：「肉」「がいしょく」等と打つと候補が絞れます。
- **読み仮名**：入力時に自動記録されるほか、設定→「読み仮名の管理」で手動追加・修正できます。

## 7. 注意・既知の制約
- **オンライン専用**：ネットが無いと開けません（PWA殻はキャッシュされますが、データ取得はネット必須）。
  → 将来「オフラインでも開けて後で同期」に拡張予定（土台は実装済み：保存は端末にもキャッシュ）。
- 無料Supabaseは**約1週間アクセスが無いと自動一時停止**します（次回アクセスで自動復帰、数秒待つことあり）。
- `config.js` のキーは**公開してよいPublishableキー**です（RLSで「自分の行だけ」に保護済み）。
  Secret/service_role は含めていません。

## 8. 秘匿帳簿が欲しくなったら（将来）
- 「別HTML」ではなく **別アカウント（別の鍵）** でログインし分けるのが正解。
  共有用アカウントとは別に、あなた専用アカウントを作れば、RLSで互いに見えません。
- もしくは、その帳簿だけ**クラウドに上げずローカル限定＋JSONバックアップ**にする方法も可。
