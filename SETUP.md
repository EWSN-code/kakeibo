## 家計簿 v1.3.1 更新手順（GitHub Pages）

### 更新
このフォルダの中身で既存リポジトリを上書き後、通常通りコミットしてください。

```bash
git add .
git commit -m "v1.3.1 Excelインポート補修とカテゴリ管理"
git push
```

### 変更点
- Excel日付の1日前ズレを修正（UTC変換を避け、ローカル日付で扱う）
- Excel取込プレビューに「口座名統合・種類」を追加
- 設定 > カテゴリ に「カテゴリ名変更」「未使用カテゴリ削除」を追加
- カテゴリ名変更は既存取引・予算・固定費・テンプレ参照も追従
- 旅費精算、医療費戻入、医療費メタ、Appleギフト、ポイント/控除の扱いを維持

### 注意
Excel読込にはオンラインで xlsx ライブラリを読み込みます。PWAキャッシュが残る場合は、一度アプリを閉じて再起動してください。
