## 家計簿 v1.7.0 更新手順

```bash
git add .
git commit -m "v1.7.0 カテゴリ月別トレンド分析"
git push
```

### 変更点
- 分析タブにカテゴリ別月次トレンドを追加
- 大・中カテゴリでは直下カテゴリを色分けした積み上げ棒グラフ
- 小カテゴリでは単系列の月別棒グラフ
- 単月表示でも選択月までの直近12か月を表示
- 複数月・任意期間では選択期間を表示
- カテゴリ名から色を固定し、順位や月が変わっても同じ色を維持
