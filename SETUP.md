## 家計簿 v1.3.7 更新手順

```bash
git add .
git commit -m "v1.3.7 金額パッドとレシート金額入力"
git push
```

### 変更点
- 金額入力欄に金額パッドを追加
- 支出入力にレシート金額入力を追加
- 金額だけを縦入力/貼り付け、選択行にカテゴリ一括設定
- 明細反映時にカテゴリごとに集約し、購入合計も自動セット
- 画面上のバージョン表示とPWA cacheを v1.3.7 に更新
