### 家計簿 v1.6.2m 更新手順

```bash
git add .
git commit -m "v1.6.2m レシート税設定と入力効率改善"
git push
```

#### 変更点
- 設定 > レシート を追加
- 税率既定は10%、食材カテゴリ配下は8%、外税端数は切り捨てを既定に変更
- 店別に税込/外税と端数処理を保存可能
- レシート入力画面から「この店の既定として保存」を追加
- カテゴリ検索で候補が1件なら自動確定
- カテゴリ検索時の読みを記録し、ひらがな入力でも候補に出やすく改善
- 支払手段選択を検索式コンボにして、口座数が多くても選びやすく改善
