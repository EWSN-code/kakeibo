## 家計簿 v1.7.0c 更新手順

```bash
git add .
git commit -m "v1.7.0c カテゴリ色設定とグラフ色統一"
git push
```

### 変更点
- カテゴリ設定に色設定ボタンと色見本を追加
- カラーピッカー、色相、彩度、明るさで調整可能
- カテゴリ色をクラウド同期対象の家計簿データへ保存
- 棒グラフ、円グラフ、凡例で同じカテゴリパスの色を統一
- 自動色へ戻す操作を追加
