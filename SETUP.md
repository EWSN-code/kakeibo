## 家計簿 v1.7.0d 更新手順

```bash
git add .
git commit -m "v1.7.0d 積立目標表示を復旧"
git push
```

### 修正点
- カテゴリ色対応時に混入した積立タイムラインの未定義変数参照を修正
- 積立目標一覧と達成タイムラインを再表示
- 目標ごとのタイムライン色は目標IDから安定生成
- v1.7.0cのカテゴリ色設定とグラフ色統一を維持
