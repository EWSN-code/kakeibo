## 家計簿 v1.6.2i 更新手順

```bash
git add .
git commit -m "v1.6.2i 現物券混在支払と支払後残高"
git push
```

### 変更点
- 支出入力の複数支払で、現物券口座も選択可能に変更
- 現物券選択時は金額ではなく枚数を入力し、平均原価で自動換算
- 現物券 + 現金 / 現物券 + カード / 現物券 + ポイントの混在支払に対応
- 支払手段ごとに現在残高、今回使用、支払後残高を表示
- 現物券は現在枚数、今回枚数、支払後枚数も表示
- 既存の現物券専用画面、未分類後回し入力、イオン外税レシート対応は維持
