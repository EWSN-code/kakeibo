### 家計簿 v1.6.2n 更新手順

```bash
git add .
git commit -m "v1.6.2n 現物券の額面履歴を追加"
git push
```

#### 変更点
- 残高・口座 > 口座編集で、現物券口座に「額面履歴」を追加
- 日付と1枚額面を登録できるように変更
- 支出で現物券を使う時は、対象日以前で最新の額面履歴を優先
- 額面履歴がない場合だけ、取引直前残高÷枚数で計算
