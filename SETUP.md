## 家計簿 v1.3.1 更新手順（GitHub Pages）

このzipの中身で既存リポジトリを上書きし、通常通り push してください。

```bash
git add .
git commit -m "v1.3.1 Excel日付補修とカテゴリ管理"
git push
```

### 変更点
- Excel日付の1日前ズレを修正
- Excel取込プレビューに「口座名統合・種類」を追加
- 設定 > カテゴリに「カテゴリ名変更」「未使用カテゴリ削除」を追加
- カテゴリ名変更は、取引・予算・固定費・テンプレの参照も追従
