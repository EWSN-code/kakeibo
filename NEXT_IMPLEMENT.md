# 家計簿アプリ ― NEXT IMPLEMENT

## v0 〜 v1（済）
- 複式エンジン／全機能（口座・前払・現物券・カード・固定費・予算・積立・ほしいもの・価格比較 …）
- CSV/JSON・オートコンプリート(読み仮名)・レシートテンプレ
- **v1：Supabaseクラウド同期＋ログイン＋PWA**（公開・共有アカウント運用中）

## v1.1 フェーズ1（今回）done ― スマホUI最適化：ナビ＆レイアウト
- [x] **下タブ＋ハンバーガー**：スマホは下部固定タブ（入力/一覧/分析/予算/≡メニュー）。
      ≡でドロワー（残高・口座/カード/ほしいもの/価格比較/レポート/設定…をグループ表示）。
      PCは従来の1段タブ（画面幅で自動切替）。
- [x] **ほしいもの・価格比較を別タブに分離**（同居をやめた）。
- [x] **入力を最優先**：起動でフォームが先頭、月次サマリは最下部に折りたたみ（既定は閉じる）。
- [x] **カテゴリ選択の改善**：候補ポップアップは2行表示（親を小さくグレー＋小カテゴリを大きく）。
      狭い画面では選択欄も**小カテゴリのみ表示**（内部値はフルパス保持）。
- [x] スマホ向けのタップ領域拡大・明細行の縦積み・モーダルの下シート化・iOS自動ズーム防止。

## v1.1 フェーズ2（次回）― 金額入力パッド（電卓＋レシート）
- [ ] 金額欄の横に **🧮ボタン**。押すと下からシート状のパッドが開く（PC/スマホ共通）。
- [ ] **電卓モード（既定）**：四則＋`( )`＋`⌫`＋`=`。IME不要で式が組める→結果を金額欄へ。
      税は専用ボタンを置かず、電卓で `×1.08` 等を打つ方針（合意済み）。
- [ ] **レシートモード**：金額を縦に連続入力→自動合計。**(あ)方式**＝数字を全部入れてから、
      行を選んでカテゴリ**一括設定**（＋「全行を1カテゴリ」）。支払手段は最後に選択。
- [ ] **テンプレに padMode（電卓/レシート）を保存**（データ構造は準備済み：`template.padMode`）。
      呼出時に自動でそのモード。CSS(pad-sheet/calc-grid/rc-*)も配置済み。

## v1.2 以降（候補）
1. オフライン完全対応（Service Workerでデータもキュー、再接続で同期）。
2. リアルタイム反映（Supabase Realtime）。
3. 閲覧専用モード（共有アカウントで編集ボタンを隠す・お母さん向け）。
4. ダッシュボード（今月の要点集約）。
5. 秘匿帳簿（別アカウント切替 / ローカル限定）。

## 設計メモ（v1.1追加分）
- ナビ: `TABS`/`TAB_LABEL`/`BOTTOM_TABS`、`renderNav(name)` で PCタブ・下タブ・ドロワー・現タブ名を同期。
  `renderDrawer()`（グループ生成）/`openDrawer/closeDrawer`。sections id は `tab-<name>`。
- タブ分離: `tab-wishlist`(ほしいもの) / `tab-price`(価格比較)。`renderWishlist`は#tab-wishlist、
  `renderPrice`は#tab-price の hidden 判定。
- カテゴリ2行: `enhanceCatSelects` の候補を `.cc-head`(親)/`.cc-leaf`(小) で描画。
  `isNarrow()` の時、選択欄表示は `catLeaf(label)` のみ（select.value はフルパス保持）。
- レイアウト: 入力サマリは `<details id="summaryFold">` に格納（既定閉）。下タブ高さ `--bottom-h`。
- 金額パッド用CSS/データ土台は配置済み（フェーズ2で有効化）。
