# 家計簿アプリ ― NEXT IMPLEMENT

## v0 〜 v1.1（済）
- 複式エンジン／全機能／Supabaseクラウド同期＋ログイン＋PWA（公開・共有アカウント運用中）
- v1.1：スマホUI最適化（下タブ＋ドロワー／入力優先／カテゴリ2行／ほしいもの・価格分離）

## v1.2（今回）done
- [x] 入力タブから今月サマリを撤去（入力に集中）
- [x] 「カテゴリ分析」→「分析」に改称
- [x] 分析セレクトを「収支 / 費用 / 収入」に（既定=収支）
- [x] 収支＝俯瞰ビュー（サマリカード＋収入/支出バー＋費用ドーナツ＋収入内訳＋掘り下げリンク）
- [x] 費用/収入＝従来のドリルダウン（予算統合・明細）を踏襲

## v1.3 フェーズ2（次に実装予定）― 金額入力パッド
- [ ] 金額欄横に🧮ボタン → 下シートで電卓/レシート。
- [ ] 電卓（既定・IME不要で式）／レシート（縦入力→自動合計→カテゴリ一括設定）。
- [ ] テンプレ padMode（電卓/レシート）で呼出時に自動切替（データ土台は準備済）。

## 設計フェーズ（v1.2完了後）
- ユーザー追加機能アイデアを棚卸し → 優先度付け → v2ロードマップ策定。

## 設計メモ（v1.2）
- 分析: `drill.kind ∈ {net, expense, income}`。`renderDrill()` が #drillBody に
  `renderOverview`(net) or `renderDrilldown`(expense/income) を描画。
  俯瞰の「掘り下げる」= `gotoDrill(kind)`（select値も同期）。
- 収入トップ集計: `M.incomeByTopCategory(state, ym)` を追加。
- 入力タブは #summaryCards/#summaryFold を持たない（renderEntryはサマリ描画なし）。
